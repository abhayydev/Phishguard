{
  "name": "phishguard-backend",
  "version": "1.0.0",
  "description": "PhishGuard backend — Vercel serverless API proxy for threat intelligence APIs",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "lint": "eslint api/**/*.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "vercel": "^34.0.0"
  }
}
